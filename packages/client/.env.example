# Base URL of the deployed game server (Socket.IO + REST API).
# Leave unset for local development (Vite proxies to localhost:3001).
# Example for production: https://your-server.onrender.com
VITE_SERVER_URL=
